{
    "adapters": {
        "dlv-dap": {
            "variables": {
                "port": "${unusedLocalPort}"
            },
            "command": [
                "$GOPATH/bin/dlv",
                "dap",
                "--listen",
                "127.0.0.1:${port}"
            ],
            "port": "${port}"
        }
    },
    "configurations": {
        "query": {
            "adapter": "dlv-dap",
            "configuration": {
                "request": "launch",
                "program": "${workspaceRoot}/cmd/query/",
                "args": [],
                "mode": "debug"
            }
        },
        "zoodle": {
            "adapter": "dlv-dap",
            "configuration": {
                "request": "launch",
                "program": "${workspaceRoot}/cmd/zoodle/",
                "args": [
                    "-d",
                    "${workspaceRoot}/.data/data/",
                    "-l",
                    "${workspaceRoot}/.data/logic/glitchless",
                    "-s",
                    "${workspaceRoot}/.data/spoiler.json"
                ],
                "mode": "debug"
            }
        },
        "test": {
            "adapter": "dlv-dap",
            "configuration": {
                "request": "launch",
                "program": "${fileDirname}/...",
                "args": [],
                "mode": "test"
            }
        }
    }
}
